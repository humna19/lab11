const pow=(a,b)=>{

    return Math.pow(a,b);


}
const round=(a)=>{
return Math.round(a);
}
const ceil=(a)=>{
return Math.ceil(a);
}
module.exports.pow=pow;
module.exports.round=round;
module.exports.ceil=ceil;

